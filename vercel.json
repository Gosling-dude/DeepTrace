{
  "buildCommand": "cd src/frontend/app && npm ci && npm run build",
  "outputDirectory": "src/frontend/app/dist",
  "installCommand": "echo 'install handled in buildCommand'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
